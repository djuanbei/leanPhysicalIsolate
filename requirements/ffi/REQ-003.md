# REQ-003 — LeanFFI matches Lean kernel semantics

LeanFFI must accept the same elaborations that the Lean kernel accepts.
Delegated to Pantograph's repl (which is the kernel).

check: kernel_semantic_match
expect: pass
