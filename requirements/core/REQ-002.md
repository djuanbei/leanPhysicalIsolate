# REQ-002 — Physical Isolation

Each instance has its own `runtime/instance_<id>/` tree with env/goals/
logs/cache/snapshots. No cross-instance state is shared.

check: isolation_integrity
expect: pass
